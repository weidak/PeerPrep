export enum Service {
    QUESTION = 'question'
}

export enum ServiceLocalPorts {
    QUESTION = '8001'
}

export enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}

export enum Role {
    ADMIN = 'admin',
    USER = 'user'
}